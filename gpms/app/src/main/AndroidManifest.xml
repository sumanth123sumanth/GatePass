<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.amrita.gpms">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_final"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Stu_login"/>
        <activity android:name=".admin_login" />
        <activity android:name=".warden_login" />
        <activity android:name=".security_login" />
        <activity
            android:name=".warden_info"
            android:noHistory="true"
            android:theme="@style/AppTheme1" />
        <activity
            android:name=".warden_approve"
            android:noHistory="true" />
        <activity
            android:name=".student_tab"
            android:label="@string/title_activity_student_tab" />
        <activity android:name=".stu_data" />
        <activity android:name=".security_info" />
        <activity android:name=".admin_info"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>